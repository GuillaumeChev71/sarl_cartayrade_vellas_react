name: Build and Deploy to FTP

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14' # Spécifiez la version de Node.js que vous utilisez

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build
      env:
        CI: false # Désactive le traitement des avertissements comme des erreurs

    - name: Install lftp
      run: sudo apt-get install lftp

    - name: Deploy to FTP
      env:
        FTP_HOST: ${{ secrets.FTP_HOST }}
        FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
        FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
      run: |
        lftp -u $FTP_USERNAME,$FTP_PASSWORD -e "
        mirror -R /home/runner/work/your-repo-name/your-repo-name/build /path/to/remote/dir;
        bye
        " $FTP_HOST
